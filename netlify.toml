[build]
  publish = "public"
  command = "npm install && yarn install && hugo"

[context.production]
  command = "npm install && yarn install && hugo"
  [context.production.environment]
    HUGO_VERSION = "0.55.6"
    HUGO_ENV = "production"
